import React, { useCallback, useState } from 'react';
import { Header, Button, Error, Form, Input, Label, LinkContainer, Success } from '@pages/SignUp/styles';

const SignUp = () => {

  return (
    <div id="container">
      <Header>myreact</Header>
      <Form>
        <h1>hi</h1>
      </Form>
    </div>
  );
};

export default SignUp;
